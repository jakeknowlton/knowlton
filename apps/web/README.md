# web

Web frontend (placeholder).

Scaffold here when ready, e.g.:

```sh
# from repo root
pnpm create vite apps/web --template react-ts
```

Set the package `"name"` to `web` so `pnpm --filter web` and the root `pnpm web`
script resolve to it. Consume the backend via `@knowlton/api-client`.
