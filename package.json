{
  "name": "knowlton",
  "version": "0.1.0",
  "description": "Knowlton monorepo: FastAPI backend, Vue web frontend, and shared TypeScript packages.",
  "private": true,
  "scripts": {
    "web": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "format": "prettier --write . && cd apps/api && uv run black . -t py314",
    "format:check": "prettier --check .",
    "lint": "eslint packages && pnpm --filter web lint",
    "lint:fix": "eslint packages --fix && pnpm --filter web lint --fix",
    "check": "pnpm format:check && pnpm lint && pnpm --filter web check && pnpm --filter @knowlton/shared check && pnpm --filter @knowlton/api-client check && pnpm api:typecheck && pnpm api:types:check",
    "verify": "pnpm check && pnpm build && pnpm api:test",
    "api": "cd apps/api && uv run fastapi dev",
    "api:test": "cd apps/api && uv run pytest",
    "api:typecheck": "cd apps/api && uv run basedpyright",
    "api:types": "cd apps/api && uv run python scripts/generate_ts_types.py ../../packages/api-client/src/generated.ts",
    "api:types:check": "cd apps/api && uv run python scripts/generate_ts_types.py ../../packages/api-client/src/generated.ts --check"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.8.0",
      "onFail": "download"
    }
  },
  "type": "module",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.5.0",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.6.0",
    "prettier": "3.8.4",
    "typescript-eslint": "^8.61.1"
  }
}
